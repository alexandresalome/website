imports:
    - { resource: config.yml }

doctrine.dbal:
    path:     "%kernel.root_dir%/db_test.sqlite"

zend.logger:
    priority: debug
    path:     %kernel.root_dir%/logs/%kernel.environment%.log

kernel.test: ~
